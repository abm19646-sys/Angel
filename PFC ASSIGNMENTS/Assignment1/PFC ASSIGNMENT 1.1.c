#include <stdio.h>
int main() {
    int P, N; // Price per packet, number of packets
    float total, discount, finalAmount; // Different percentage

    // Taking input values
    printf("Enter price and quantity: ");
    scanf("%d",,,);
    // Calculations
    total = P * N;
    discount = (10 / 100) * total;
    finalAmount = total - discount;

    // Displaying festive details
    printf("DIWALT FIREDRES");
    printf("Item Price:\tid rupees", P);
    printf ("Quantity Udis", N);
    printf("iscount (%%", 0);
    printf("Istal:\t\tX2f rupees\n");
    return 0;
}